language: ruby
before_script: 
#  - sudo apt-get -qq update 
#  - sudo apt-get source tumiki-fighters
  - git clone --depth=1 https://github.com/brownman/travis_screencast
  - cd travis_screencast
  - echo let us start  
  - chmod u+x *.sh . -R
  - mkdir /tmp/log
  - mkdir /tmp/PRODUCT
  - ./travis.sh
  
after_script:
  - echo done
